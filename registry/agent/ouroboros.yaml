id: ouroboros
name: Ouroboros
category: agent
subcategory: agent-framework
repo_url: https://github.com/Q00/ouroboros
license: MIT
primary_language: Python
tagline: 'Agent OS: Stop prompting.'
description: 'Ouroboros — Agent OS: Stop prompting. Start specifying. Upstream maintainer: Q00.


  In the atlas: this is an agent-orchestration entry. The harness paradigm reads agent frameworks as the policy + tool-routing layer that sits between intent and the model tier.'
maturity: ga
maintainer:
  type: community
  name: Q00
origin_country: null
model_agnostic_score: 5
sovereignty_notes: 'Q00 (community / individual) ships Ouroboros as MIT-licensed Python under the PyPI name `ouroboros-ai`, with a multi-runtime adapter that targets Claude Code, Codex CLI, Copilot CLI, OpenCode, Hermes, Gemini, Kiro and Pi plus a LiteLLM extra, so the provider surface is meaningfully multi-vendor (model-agnostic 4: OpenAI-compatible pluggable, local first-class via LiteLLM). Alignment is `aligned`: the project pitches itself as an Agent OS with a Seed/Ledger/EventStore (audit + replay), a 3-stage Mechanical→Semantic→Consensus evaluation gate (output contract), an interview→seed→execute policy loop and a stable governance-prompt spec layer.'
harness_paradigm_alignment: aligned
adjacent_to:
- langgraph
- sre
- tabularis
- tribe
- trpc-agent-go
