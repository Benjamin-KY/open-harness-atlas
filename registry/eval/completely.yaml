id: completely
name: Completely
category: eval
subcategory: agent-eval
repo_url: https://github.com/23ag1/completely
homepage: https://github.com/23ag1/completely
license: MIT
primary_language: Shell
tagline: Quality-first harness for autonomous AI coding agents — deterministic gates + a default-FAIL evaluator over a Beads task spine.
description: 'Completely — Quality-first harness for autonomous AI coding agents — deterministic gates + a default-FAIL evaluator over a Beads task spine. Claude Code plugin; done is earned, not asserted. Upstream maintainer: 23ag1.


  In the atlas: this is an evaluation harness. Evaluation is the feedback loop that keeps every other harness honest — if you cannot measure the behaviour of the swap target, the swap is not really safe.'
maturity: beta
maintainer:
  type: community
  name: 23ag1
origin_country: null
model_agnostic_score: 1
sovereignty_notes: '23ag1 (community / individual maintainer) ships `completely` under MIT as a Claude Code plugin: deterministic gates (lint/types, dangerous-cmd, write-zone) + code-reviewer + security-reviewer + an independent default-FAIL evaluator that runs in a separate read-only process and returns FAIL unless evidence proves pass, over a Beads task spine. Model-agnostic 1 reflects the Claude Code coupling — the plugin marketplace install, CLI surface, and slash commands are Claude-Code-specific today. Output-contract is native (`default-FAIL = malformed-or-unverified-output rejected, not warned about`), policy-router partial (gates + dangerous-cmd allow/deny enforced before tool execution), audit-log partial (Beads tasks + commit/close as a deterministic spine).'
harness_paradigm_alignment: partial
adjacent_to:
- holon-bench
- keel
- langeval
- lm-evaluation-harness
- mcp-server
five_component_coverage:
  policy_router: partial
  source_authority: none
  prompt_composer: none
  output_contract: native
  audit_log_fsm: partial
