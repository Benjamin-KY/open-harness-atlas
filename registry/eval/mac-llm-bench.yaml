id: mac-llm-bench
name: Mac LLM Bench
category: eval
subcategory: benchmark
repo_url: https://github.com/enescingoz/mac-llm-bench
license: MIT
primary_language: Shell
tagline: Community benchmark database for running LLMs on Apple Silicon Macs
description: 'Mac LLM Bench — Community benchmark database for running LLMs on Apple Silicon Macs. Upstream maintainer: enescingoz.


  In the atlas: this is an evaluation harness. Evaluation is the feedback loop that keeps every other harness honest — if you cannot measure the behaviour of the swap target, the swap is not really safe.'
maturity: beta
maintainer:
  type: community
  name: enescingoz
origin_country: null
model_agnostic_score: 3
sovereignty_notes: 'Single-maintainer (enescingoz; MIT) reproducible benchmark suite and result database for running GGUF (via llama.cpp''s `llama-bench`) and MLX models on Apple Silicon, plus a HumanEval+ code-quality runner — results tabled by chip generation (M1-M5) for cross-rig comparison. Model-agnostic 3 because the runner is local-weights-only by design (its whole point is on-device measurement), but it is model-pluggable across any GGUF / MLX checkpoint. Alignment ''partial'': it is a focused performance + quality eval — no agent or governance surface.'
harness_paradigm_alignment: partial
adjacent_to:
- lm-evaluation-harness
- play-bench
- precisionmembench
- scaffold-bench
- seci
deployment_posture: local-first
