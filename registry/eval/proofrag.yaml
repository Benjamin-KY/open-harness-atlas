id: proofrag
name: Proofrag
category: eval
subcategory: llm-judge
repo_url: https://github.com/unshDee/proofrag
homepage: https://pypi.org/project/proofrag/
license: MIT
primary_language: Python
tagline: Point your agent at your docs and your RAG app; get a golden test set + an LLM-as-judge & retrieval scorecard, in one command.
description: 'Proofrag — Point your agent at your docs and your RAG app; get a golden test set + an LLM-as-judge & retrieval scorecard, in one command. Upstream maintainer: unshDee.


  In the atlas: this is an evaluation harness. Evaluation is the feedback loop that keeps every other harness honest — if you cannot measure the behaviour of the swap target, the swap is not really safe.'
maturity: beta
maintainer:
  type: community
  name: unshDee
origin_country: null
model_agnostic_score: 4
sovereignty_notes: 'unshDee (community / individual) maintains proofrag on PyPI under MIT as both a CLI and an open-standard Agent Skill (agentskills.io) for Claude Code / Codex / Cursor — its loop is `generate → validate → run → evaluate → report` with an explicit CI gate. Model-agnostic 4: the `[anthropic]` and `[openai]` (OpenAI-compatible / local Ollama) extras make provider a config string, and the project wraps the eval loop rather than reinventing metrics. Source authority and output contract score partial because citation-quality scoring and the threshold-driven exit code are real, but the gate is single-axis and there is no formal policy router or FSM-style escalation.'
harness_paradigm_alignment: partial
adjacent_to:
- lm-evaluation-harness
- ragaliq
- refusebench
- research-sweeper
- self-healing-rag
deployment_posture: local-first
