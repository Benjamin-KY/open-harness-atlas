id: scaffold-bench
name: Scaffold Bench
category: eval
subcategory: benchmark
repo_url: https://github.com/1337hero/scaffold-bench
license: MIT
primary_language: TypeScript
tagline: Local LLM BenchMarking
description: 'Scaffold Bench — Local LLM BenchMarking. Upstream maintainer: 1337hero.


  In the atlas: this is an evaluation harness. Evaluation is the feedback loop that keeps every other harness honest — if you cannot measure the behaviour of the swap target, the swap is not really safe.'
maturity: beta
maintainer:
  type: community
  name: 1337hero
origin_country: null
model_agnostic_score: 4
sovereignty_notes: 1337hero (individual maintainer; MIT). Bun/TypeScript coding-bench that grades not just correctness but reviewer-behavioural quality (touched files, idiomatic patches, verify-command discipline) with deterministic scoring against a real filesystem diff. MAS=4 — first-class local backends (Ollama 11434, llama.cpp / llama-swap 8082, LM Studio 1234, vLLM) plus OpenRouter for hosted models, all via OpenAI-compatible endpoints; per-model preflight token classifier. Harness alignment partial — explicit tool-call harness as a first-class axis (three contracts), deterministic output_contract via actually running tests; no policy_router or refusal FSM.
harness_paradigm_alignment: partial
adjacent_to:
- agent-arena
- lm-evaluation-harness
- seci
- tau2-bench
deployment_posture: local-first
