id: vigil-llm
name: Vigil
category: governance
subcategory: llm-firewall
repo_url: https://github.com/deadbits/vigil-llm
license: Apache-2.0
primary_language: Python
tagline: 'REST API + Python library for LLM prompt security scanning: YARA/heuristics, vector-DB similarity, transformer classifier, canary tokens'
description: |
  Vigil is Adam Swanda's rEST API + Python library for LLM prompt security scanning: YARA/heuristics, vector-DB similarity, transformer classifier, canary tokens. Canary token leakage detection for goal-hijacking; multi-scanner layered approach.

  In the atlas: Vigil is a governance-layer entry. Where it lands on the five-component spine (policy router / source authority / prompt composer / output contract / audit-log FSM) is captured in `five_component_coverage` below.
maturity: alpha
maintainer:
  type: individual
  name: Adam Swanda
origin_country: US
model_agnostic_score: 4
five_component_coverage:
  policy_router: partial
  source_authority: none
  prompt_composer: none
  output_contract: partial
  audit_log_fsm: partial
sovereignty_notes: |
  Adam Swanda (independent maintainer, US); OSS core is Apache-2.0 and fully self-hostable. Model-agnostic 4 — the substrate is provider-abstracted but with some defaults or shipped configurations that favour a specific tier.
harness_paradigm_alignment: aligned
