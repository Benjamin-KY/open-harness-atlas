id: exo
name: exo
category: routing
subcategory: local-inference-server
repo_url: https://github.com/exo-explore/exo
license: Apache-2.0
primary_language: Python
tagline: 'Run frontier AI by clustering consumer devices into a P2P inference pool with OpenAI-compat API'
description: |
  exo is exo-explore's run frontier AI by clustering consumer devices into a P2P inference pool with OpenAI-compat API. Model-parallel sharding across heterogeneous devices; dynamic topology discovery.

  In the atlas: exo is a routing-layer entry — it makes the model tier swappable so any governance / agent / eval / redteam stack above it stays sovereign-by-construction.
maturity: beta
maintainer:
  type: community
  name: exo-explore
origin_country: US
model_agnostic_score: 5
sovereignty_notes: |
  exo-explore (community-maintained, US); OSS core is Apache-2.0 and self-hostable as a Python package or container; OpenAI-compatible local backends are first-class. Model-agnostic 5 — provider is a swap target and the same code path runs against every supported backend.
harness_paradigm_alignment: partial
adjacent_to:
  - mlc-llm
  - shimmy
  - web-llm
