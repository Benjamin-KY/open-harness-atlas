id: lmdeploy
name: LMDeploy
category: routing
subcategory: local-inference-server
repo_url: https://github.com/InternLM/lmdeploy
license: Apache-2.0
primary_language: Python
tagline: 'LLM compression + deployment toolkit with TurboMind engine and OpenAI-compat serving API'
description: |
  LMDeploy is Shanghai AI Lab (InternLM)'s lLM compression + deployment toolkit with TurboMind engine and OpenAI-compat serving API. TurboMind custom CUDA engine; AWQ/GPTQ quantization.

  In the atlas: LMDeploy is a routing-layer entry — it makes the model tier swappable so any governance / agent / eval / redteam stack above it stays sovereign-by-construction.
maturity: ga
maintainer:
  type: company
  name: Shanghai AI Lab (InternLM)
origin_country: CN
model_agnostic_score: 5
sovereignty_notes: |
  Shanghai AI Lab (InternLM) (CN-incorporated company); OSS core is Apache-2.0 and self-hostable as a Python package or container; OpenAI-compatible local backends are first-class. Model-agnostic 5 — provider is a swap target and the same code path runs against every supported backend.
harness_paradigm_alignment: partial
adjacent_to:
  - deepspeed-mii
  - litellm
  - llama-cpp
  - ollama
  - openllm
  - sglang
  - vllm
  - xinference
