id: local-ai-toolkit
name: Local AI Toolkit
category: routing
subcategory: llm-gateway
repo_url: https://github.com/wa91h/local-ai-toolkit
license: MIT
primary_language: Go Template
tagline: A self-hosted AI toolkit running locally via Docker Compose, bundling an LLM gateway, workflow automation, and a chat UI — all backed by a shared PostgreSQL database.
description: 'Local AI Toolkit — A self-hosted AI toolkit running locally via Docker Compose, bundling an LLM gateway, workflow automation, and a chat UI — all backed by a shared PostgreSQL database. Upstream maintainer: wa91h.


  In the atlas: this is a routing-layer entry. The harness paradigm requires a swap target — provider abstractions, gateways, and OpenAI-compatible proxies are the substrate that makes the swap practical.'
maturity: beta
maintainer:
  type: community
  name: wa91h
origin_country: null
model_agnostic_score: 3
sovereignty_notes: wa91h (community maintainer) ships AI Local Toolkit under MIT as a Docker-Compose + Helm bundle around LiteLLM, n8n and Open WebUI on PostgreSQL, with an explicit production-checklist disclaimer about missing TLS, auth and secrets management. Model-agnostic 5 — the LiteLLM admin UI lets you register any provider and use it as an OpenAI-compatible endpoint from any consumer in the stack, so provider really is a config string. Curator-reviewed; sits in routing as a sovereign local-deployment recipe.
harness_paradigm_alignment: partial
adjacent_to:
- litellm
- model-flux
- nadirclaw
- open-next-router
- opencode-models-discovery
deployment_posture: local-first
