id: model-flux
name: Model Flux
category: routing
subcategory: llm-gateway
repo_url: https://github.com/dabao-yi/model-flux
license: MIT
primary_language: TypeScript
tagline: 'ModelFlux: OpenAI-compatible model traffic router with health-aware key-pool scheduling and failover'
description: |-
  Model Flux — ModelFlux: OpenAI-compatible model traffic router with health-aware key-pool scheduling and failover. Upstream maintainer: dabao-yi.

  In the atlas: this is a routing-layer entry. The harness paradigm requires a swap target — provider abstractions, gateways, and OpenAI-compatible proxies are the substrate that makes the swap practical.
maturity: beta
maintainer:
  type: community
  name: dabao-yi
origin_country: null
model_agnostic_score: 4
sovereignty_notes: dabao-yi (community-maintained); OSS core is MIT License and self-hostable per the upstream README. Model-agnostic 4 — multi-provider, OpenAI-compatible-endpoint pluggable per the README; local backends explicitly supported. Sovereignty score is a conservative auto-curated default — refine on first manual review of the README.
harness_paradigm_alignment: partial
adjacent_to:
- litellm
- nadirclaw
- open-next-router
- otoroshi-llm-extension
- windsurfapi
