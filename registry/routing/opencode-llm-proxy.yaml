id: opencode-llm-proxy
name: Opencode LLM Proxy
category: routing
subcategory: ai-gateway
repo_url: https://github.com/KochC/opencode-llm-proxy
homepage: https://www.npmjs.com/package/opencode-llm-proxy
license: MIT
primary_language: JavaScript
tagline: Local AI gateway for OpenCode — use any model via OpenAI, Anthropic, or Gemini API
description: 'Opencode LLM Proxy — Local AI gateway for OpenCode — use any model via OpenAI, Anthropic, or Gemini API. Upstream maintainer: KochC.


  In the atlas: this is a routing-layer entry. The harness paradigm requires a swap target — provider abstractions, gateways, and OpenAI-compatible proxies are the substrate that makes the swap practical.'
maturity: beta
maintainer:
  type: community
  name: KochC
origin_country: null
model_agnostic_score: 4
sovereignty_notes: 'KochC (community / individual) publishes opencode-llm-proxy on npm under MIT as an OpenCode plugin that starts a local HTTP server (127.0.0.1:4010) translating between OpenAI Chat Completions, OpenAI Responses, Anthropic Messages and Google Gemini APIs over the top of OpenCode''s configured backends (GitHub Copilot, Anthropic, Gemini, Ollama, OpenRouter, Bedrock, etc.), all with streaming. Model-agnostic 5: provider is a config string at the OpenCode layer, and the same proxy path runs commercial, hosted-OSS and local backends without code changes — local Ollama is treated as first-class alongside vendor APIs.'
harness_paradigm_alignment: partial
adjacent_to:
- apisix
- aqbot
- litellm
- routerly
deployment_posture: local-first
