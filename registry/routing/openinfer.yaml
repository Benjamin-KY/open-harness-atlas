id: openinfer
name: Openinfer
category: routing
subcategory: serving
repo_url: https://github.com/openinfer-project/openinfer
homepage: https://open-infer.org/
license: Apache-2.0
primary_language: Rust
tagline: Pure Rust + CUDA LLM inference engine
description: 'Openinfer — Pure Rust + CUDA LLM inference engine. Upstream maintainer: openinfer-project.


  In the atlas: this is a routing-layer entry. The harness paradigm requires a swap target — provider abstractions, gateways, and OpenAI-compatible proxies are the substrate that makes the swap practical.'
maturity: ga
maintainer:
  type: community
  name: openinfer-project
origin_country: null
model_agnostic_score: 3
sovereignty_notes: openinfer-project is a community organisation shipping openinfer as an Apache-2.0 pure-Rust + CUDA local LLM inference engine (no PyTorch, no ONNX, every kernel hand-written) targeting frontier-scale local models (Qwen3, Qwen3.5, DeepSeek V4 Flash, Kimi-K2). Model-agnostic 4 — exposes OpenAI-compatible `/v1/completions` (and streaming), so any OpenAI SDK swaps baseURL and runs the same code path against local weights. Pushed 2026-06-14.
harness_paradigm_alignment: partial
adjacent_to:
- litellm
- parallax
- powerserve
- serverlessllm
deployment_posture: local-first
