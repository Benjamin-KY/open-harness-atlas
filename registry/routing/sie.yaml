id: sie
name: Sie
category: routing
subcategory: inference-server
repo_url: https://github.com/superlinked/sie
homepage: https://superlinked.com
license: Apache-2.0
primary_language: Python
tagline: Open-source inference server and production cluster for all the models your agent needs.
description: 'Sie — Open-source inference server and production cluster for all the models your agent needs. Upstream maintainer: superlinked.


  In the atlas: this is a routing-layer entry. The harness paradigm requires a swap target — provider abstractions, gateways, and OpenAI-compatible proxies are the substrate that makes the swap practical.'
maturity: ga
maintainer:
  type: company
  name: superlinked
origin_country: null
model_agnostic_score: 3
sovereignty_notes: Superlinked (commercial vendor at superlinked.com); Apache-2.0 inference engine for 85+ embedding/reranking/extraction models with three-call SDK (`encode`/`score`/`extract`), hot-swap and LRU eviction. Model-agnostic 4 — drop-in OpenAI-compatible `/v1/embeddings`, local Docker first-class, integrates with LangChain/LlamaIndex/Haystack/DSPy/CrewAI; ships full production stack (gateway, KEDA autoscaling, Grafana, Terraform). Anonymous telemetry on by default (`SIE_TELEMETRY_DISABLED=1` to opt out).
harness_paradigm_alignment: partial
adjacent_to:
- litellm
- spiceai
- tiny-vllm
- vllm-qwen
- xllm
