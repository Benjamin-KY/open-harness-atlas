id: xinference
name: Xinference
category: routing
subcategory: local-inference-server
repo_url: https://github.com/xorbitsai/inference
license: Apache-2.0
primary_language: Python
tagline: Distributed inference service for LLMs, embeddings, image-gen, speech - swap model with one API call
description: 'Xinference is Xorbits AI''s distributed inference service for LLMs, embeddings, image-gen, speech - swap model with one API call. Multi-engine (vLLM/llama.cpp/transformers); RESTful + OpenAI-compat.


  In the atlas: Xinference is a routing-layer entry — it makes the model tier swappable so any governance / agent / eval / redteam stack above it stays sovereign-by-construction.

  '
maturity: ga
maintainer:
  type: company
  name: Xorbits AI
origin_country: CN
model_agnostic_score: 5
sovereignty_notes: 'Xorbits AI (CN-incorporated company); OSS core is Apache-2.0 and self-hostable as a Python package or container; OpenAI-compatible local backends are first-class. Model-agnostic 5 — provider is a swap target and the same code path runs against every supported backend.

  '
harness_paradigm_alignment: partial
adjacent_to:
- deepspeed-mii
- lmdeploy
- openllm
- sglang
- vllm
deployment_posture: local-first
